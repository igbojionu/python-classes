import random

# Generate a random number between 1 and 100
random_number = random.randint(1, 100)
choice = random.choice(['win', 'lose', 'draw'])

# Print the random number
print(random_number)
print (choice)
#generate random number until 50tries the showa a win else keeps showing loose or draw
#Examples¶ implement all Basic examples: in this url =  { https://docs.python.org/3/library/random.html?highlight=randint#examples }