import random

# Generate a random number between 1 and 100
random_number = random.randint(1, 100)
choice = random.choice(['win', 'lose', 'draw'])

# Print the random number
print(random_number)
print (choice)
